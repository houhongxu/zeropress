{
  "name": "easypress",
  "version": "0.0.1",
  "description": "easier SSG framework",
  "scripts": {
    "build:comment": "echo 多入口打包，tsup不打包node_modules，--shims兼容esm与cjs的全局变量如__dirname，--no-splitting兼容shims，让shims不分割为chunk影响__dirname等路径，npm run build --watch无法接受watch参数，pnpm又固定了包管理器，所以复制build命令到dev",
    "build": "tsup ./src/node/cli.ts ./src/node/index.ts --shims --no-splitting --format cjs,esm --dts  --outDir=./lib",
    "dev": "cross-env NODE_ENV=development tsup ./src/node/cli.ts ./src/node/index.ts --shims --no-splitting --format cjs,esm --dts  --outDir=./lib --watch",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "type": "module",
  "main": "./lib/index.cjs",
  "module": "./lib/index.js",
  "types": "./lib/index.d.cts",
  "bin": "./bin/easypress.js",
  "files": [
    "./lib",
    "./bin"
  ],
  "dependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "commander": "^11.1.0",
    "fs-extra": "^11.2.0",
    "react-router-dom": "^6.22.0",
    "fast-glob": "^3.3.2",
    "vite": "^5.0.8"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "cross-env": "^7.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "tsup": "^8.0.1"
  },
  "peerDependencies": {
    "react": ">=18.0.0 <19.0.0",
    "react-dom": ">=18.0.0 <19.0.0"
  }
}
