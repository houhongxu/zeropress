import { defineConfig } from '../dist'

export default defineConfig({
  title: 'xxx',
  themeConfig: {
    nav: [
      { text: '主页', link: '/' },
      { text: '文档', link: '/docs/' },
    ],
  },
})
